# LoneStar AI Website

Static marketing site for LoneStar AI Assistant Setup.

Initial hosting target: GitHub Pages.

## Mission Control

`mission.html` is a private AI crew dashboard prototype. Static mode is safe to preview, but real login/private messaging requires the Node backend wrapper in `server/mission-server.js`.

See `server/README.md` for local password setup and run instructions.
